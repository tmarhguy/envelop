# Copy to .env and fill in. NEVER commit .env (gitignored).
ENVELOP_SUPABASE_URL='https://YOUR_PROJECT.supabase.co'
ENVELOP_SUPABASE_KEY='YOUR_PUBLISHABLE_KEY'
# Server-side ONLY (dashboard / local scripts). NEVER ship in apps.
ENVELOP_SUPABASE_SECRET='YOUR_SECRET_KEY'
